# -*- coding: utf-8 -*-
import os

ti_mian = """# 附加题 · 代号「MAYDAY」

> **题型**：Misc / Reverse / Pwn 综合 · **分值**：150 分 · **难度**：★★★★★
> **性质**：附加题。不计入常规赛排名，单独设奖。做不出不影响正赛成绩。
> **特别要求**：本机物理机运行，需关闭虚拟化，需常驻运行本赛事反作弊程序。

---

## 背景

社团的飞行训练服务器最近有点不对劲。

值夜的同学说，雷达上偶尔会飘过一些\"不该存在的目标\"——它们以民航机绝无可能的速度掠过，有时候干脆从地底下钻出来。地勤记录里管这叫「幽灵航迹」（Ghost Track）。

老社员们对此讳莫如深，只留下一句话：

> *\"模拟器不会撒谎。但撒谎的方式，有很多种。\"*

你被授权接入训练服务器。两个任务，两个 flag。

祝好运。

---

## 〇、环境准备（必读，做不完这一步后面全是零）

### 0.1 硬件与系统要求

| 项目 | 要求 |
|---|---|
| 运行方式 | **物理机**，不得在虚拟机内运行 |
| 虚拟化 | **必须关闭**（见 0.2） |
| 操作系统 | Ubuntu 22.04 LTS（推荐，提供预装镜像）/ Windows 10 1809+ |
| 显卡 | 支持 OpenGL 3.3+，显存 ≥ 2GB |
| 内存 | ≥ 8 GB |
| 磁盘 | ≥ 20 GB 可用空间 |
| 网络 | 需 UDP 出网，可访问训练服务器 |

### 0.2 关闭虚拟化

反作弊会在启动时检测虚拟化环境。**检测到虚拟化将直接拒绝启动**，没有例外。

请**在开赛前**完成以下操作并用自检工具验证：

**Windows**

1. 关闭 Hyper-V（管理员权限，执行后重启）：
   ```
   bcdedit /set hypervisorlaunchtype off
   ```
2. 关闭「核心隔离 / 内存完整性」：Windows 安全中心 → 设备安全性 → 核心隔离 → 内存完整性 → 关
3. 关闭 WSL2（如已安装）：
   ```
   wsl --shutdown
   dism.exe /Online /Disable-Feature:Microsoft-Windows-Subsystem-Linux
   ```
4. 关闭虚拟机平台：`dism.exe /Online /Disable-Feature:VirtualMachinePlatform`
5. BIOS 中关闭 VT-x / SVM（如上述步骤后仍被拦截）

**Linux**

1. 卸载 kvm 模块：`sudo modprobe -r kvm_intel kvm`（或 `kvm_amd`）
2. BIOS 中关闭 VT-x / SVM
3. 如使用 Docker，需停止相关服务

> **注意**：部分杀毒软件与第三方反作弊自带虚拟化层。如遇拦截，请携带自检工具输出联系组委会，我们将人工判定是否放行。

### 0.3 安装与自检

```bash
# 1. 拉取赛事环境（含 FlightGear 与反作弊，已锁定版本）
./install.sh

# 2. 环境自检（检查虚拟化、依赖、版本、文件完整性）
./anticheat-selfcheck

# 3. 看到 [OK] Environment ready 后方可开始
```

自检未通过时，工具会**明确指出是哪一项不合格**并给出修复建议。请勿跳过。

### 0.4 启动

```bash
./launcher.sh --callsign=<你的队伍ID>
```

脚本会自动完成：参数校验 → 文件完整性校验 → 启动反作弊 → 启动 FlightGear → 注册训练服务器。

**反作弊窗口请勿关闭。** 关闭即断开心跳，当前会话全部作废。

看到 FlightGear 主界面与右上角 `CONNECTED` 指示灯后，即可开始。

---

## 一、规则

### 允许

- ✅ 使用 FlightGear **内置自动驾驶（AP）** 及一切正常飞行手段
- ✅ 使用任意外部脚本 / 程序读取模拟器状态、驱动操纵面
- ✅ 查阅 FlightGear 官方文档、Wiki、源码
- ✅ 使用 AI 工具辅助
- ✅ 反编译、调试、修改本机上的任何东西
- ✅ 在练习模式（`./launcher.sh --training`）下任意尝试，**不计次、不记录**

### 禁止

- ❌ 在虚拟机内运行
- ❌ 关闭、杀死、挂起反作弊进程
- ❌ 攻击训练服务器、其他选手的客户端，或任何非本机的系统
- ❌ 多人共用一个 callsign（flag 与队伍 ID 绑定，答案共享无效）
- ❌ 在公共频道进行影响他人的操作

### 关于「使用 AI」

我们不禁止，也无意禁止。规则层会告诉你：这道题意图考的东西，AI 帮得上忙，但帮不到底。

---

## 二、任务

### Task 1 · 飞抵上海张江（50 分）

**「先把飞机安全送到。」**

从**浦东国际机场 (ZSPD)** 起飞，飞往上**海科技大学张江校区**（大致坐标 31.177°N, 121.596°E），在目标点上方稳定悬停足够时间。

**要求**

- 起飞点：浦东机场跑道（服务器公告给出）
- 目标点：上科大张江校区附近
- 抵达判据：进入目标点水平半径 **500m** 内，高度低于 **1000 ft**，持续 **≥ 5s**
- **允许全程使用内置 AP**——不会手动飞行不构成障碍

**提交流程**

1. 达成抵达条件后，服务器自动生成一段 **ATC 语音**（base64 编码 WAV 文件）下发给你
2. 这段语音中**隐藏了 flag key**（编码方式另行通知，可能为 base64 / AES / RSA 分级挑战）
3. 解密获得 key，提交到独立 Web 界面
4. Web 验证 key 正确 → 返回 flag1

**得分**：抵达 + 正确解密 key → 50 分

---

### Task 2 · 幽灵航迹（100 分）

**「模拟器不会撒谎。但撒谎的方式，有很多种。」**

在**默认飞行模型（JSBSim）**、**反作弊正常运行**的前提下，让模拟器**稳定地**报出超越机体结构极限的飞行状态，并让反作弊为这段飞行签出有效票据。

**三个检查点**（选做，每项独立计分）：

| 代号 | 条件 | 需持续 | 分值 |
|---|---|---|---|
| **飞天** | 气压高度 > 100,000 ft | ≥ 10 秒 | 33 分 |
| **遁地** | 离地高度 (AGL) < −300 ft | ≥ 10 秒 | 33 分 |
| **超速** | 空速 > 3 × VNE（c172p 的 VNE = 163 kias） | ≥ 15 秒 | 34 分 |

**要求**

- 状态必须**连续、平滑**。瞬移、跳变、单帧尖峰一律不认
- 必须由**正常运行的反作弊**为此段飞行签出有效票据
- 达成任意 1 个检查点 = 20 分，2 个 = 50 分，3 个 = 100 分（阶梯加分）

**说明**

- 训练服务器会为本题提供**独立频道**，请在独立频道内操作（公共频道内请不要表演）
- 本题**不限制**你如何实现。这是附加题的附加题。
- 提示：FlightGear 有若干「后门」飞行模型可用，但反作弊会检测。真正的挑战在于绕过反作弊本身。

**提交**：达成后服务器自动判定，通过后 flag 在排行榜显示。
"""

with open(r'c:\Users\steven\Documents\HW\gkp2026\出题\flightgear-ctf\doc\飞行模拟CTF-题面.md', 'w', encoding='utf-8') as f:
    f.write(ti_mian)
print('题面.md written OK')
